import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TambahpengaduanPage } from './tambahpengaduan.page';

describe('TambahpengaduanPage', () => {
  let component: TambahpengaduanPage;
  let fixture: ComponentFixture<TambahpengaduanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TambahpengaduanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
