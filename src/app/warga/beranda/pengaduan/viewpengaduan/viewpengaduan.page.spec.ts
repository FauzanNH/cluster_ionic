import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewpengaduanPage } from './viewpengaduan.page';

describe('ViewpengaduanPage', () => {
  let component: ViewpengaduanPage;
  let fixture: ComponentFixture<ViewpengaduanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewpengaduanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
